# RAG workstream dependencies (conda env: rag, Python 3.11)
# Install: conda activate rag && pip install -r requirements-rag.txt

# Vector DB
chromadb>=0.4.22

# Embedding model (BGE-M3 via sentence-transformers)
sentence-transformers>=2.7.0

# Crawling
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.27.0
requests>=2.31.0

# LLM
openai>=1.0.0

# Utils
tqdm>=4.65.0
python-dotenv>=1.0.0
